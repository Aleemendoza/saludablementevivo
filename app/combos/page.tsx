import { combos } from "@/lib/catalog/data";
import { comboImages } from "@/lib/catalog/images";

export const metadata = { title: "Combos saludables | Saludablemente Vivo" };
export default function CombosPage() { const groups = [...new Set(combos.map((combo) => combo.group))]; return <main><section className="section"><p className="eyebrow">COMBOS SALUDABLES</p><h1>Elegí por tu<br /><em>momento.</em></h1><p className="lead">Todos se construyen con nuestros 15 productos base, para que la selección sea simple y siempre fresca.</p>{groups.map((group) => <div key={group} className="combo-group"><h2>{group}</h2><div className="offer-grid">{combos.filter((combo) => combo.group === group && combo.name !== "Energía para Oficina").map((combo) => <article className="offer-card" key={combo.name}><img src={comboImages[combo.name]} alt={`Productos incluidos en ${combo.name}`} /><div className="offer-content"><p className="eyebrow">AHORRÁS {combo.discount}%</p><h3>{combo.name}</h3><p>{combo.purpose}</p><ul>{combo.items.map((item) => <li key={item}>{item}</li>)}</ul><button className="button primary">Elegir combo <span>→</span></button></div></article>)}</div></div>)}</section></main>; }
